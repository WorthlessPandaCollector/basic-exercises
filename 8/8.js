// declare a function that takes in a single argument // a name 







const myObject = {                  // declared object
    sayHello(name){
        if (name === "tums"|| name === "pickle" || name === "interesting" || name === "@diply"){  // if statement, name ===   || means or
            console.log("Fuck you, " + name);  // log
        }
        else {      // else
            console.log("howdy tharr, " + name);  // log
        }
    }
}

myObject.sayHello("tim");




//  this wasn't that hard once i learned what || could be used fOR. 
